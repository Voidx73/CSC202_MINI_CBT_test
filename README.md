# Mini CBT Engine
**Student Name:** umar garba suru
**Course:** CSC202_principle of programming
**Matric:** MAAUN/24/CSC/0069

## Description
A simple Computer Based Testing (CBT) web application built to demonstrate Python's web capabilities and Object-Oriented Programming.

## Technologies
- **Python:** Core logic.
- **Flask:** Web framework.
- **OOP:** Question model class.
- **Datetime:** Tracking session duration.

## Installation
1. Install Flask:
   `pip install flask`

## Running the Project
1. Open your terminal in the project folder.
2. Run the command:
   `python app.py`
3. Open `http://127.0.0.1:5000` in your browser.

## GitHub Instructions
1. Initialize Git: `git init`
2. Add files: `git add .`
3. **Commit 1:** `git commit -m "Initial structure and models"`
4. **Commit 2:** `git commit -m "Added Flask routes and templates"`
5. Connect to GitHub: `git remote add origin <your-repo-url>`
6. Push: `git push -u origin main`
7.